const canvasSize = 640
const maxDelta = 1/60
const maxFPS = 60
const movingSpeed = {
  hero: 0.4
}

const keyboard = {
  LEFT: 37,
  RIGHT: 39,
  UP: 38,
  DOWN: 40,
}

export {
  canvasSize,
  maxDelta,
  keyboard,
  maxFPS,
  movingSpeed,
}

