const canvasSize = 640
const maxDelta = 1/30
const maxFPS = 30
const movingSpeed = {
  hero: 0.2
}

const keyboard = {
  LEFT: 37,
  RIGHT: 39,
  UP: 38,
  DOWN: 40,
}

export {
  canvasSize,
  maxDelta,
  keyboard,
  maxFPS,
  movingSpeed,
}

